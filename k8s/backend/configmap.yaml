apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: task-management-platform
  labels:
    app: backend
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_USER: postgres
  DB_NAME: task_manager